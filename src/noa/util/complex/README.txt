NOTE:
=====

This is the implementation for Noa::Complex<> types. These header files should not be included directly,
especially because the include order matters... They are all included by util/Complex.h

It is mostly adapted from thrust/complex.h. See licence/nvidia_thrust.txt.
