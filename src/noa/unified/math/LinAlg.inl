#include "noa/unified/Array.h"

namespace noa::cpu::math {
    template<typename T, typename U, typename>
    void lstsq(const Array<T>& a, const Array<T>& b, float cond, const Array<U>& svd) {

    }
}
